let email = document.getElementById("email")
let password = document.getElementById("password")
let con = document.getElementById("con")
let accueil = document.getElementById("accueil")

email.addEventListener("submit",(event)=>{ //pass automatiquement , addEventListener a deux parametres ,les input prennent des value
  even.preventDefault() //te previent de remplir correctement,    required pour verifier que le champ n'est pas vide
})

password.addEventListener("submit",(event)=>{
  even.preventDefault()
})
                                                                                             